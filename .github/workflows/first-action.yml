name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: windows-latest
    steps:
      - name: Print Greeting
        run: |
          echo "hello World"
          echo "Hope Everthing's Fine"
      - name: Print GoodBye
        run: echo "Done - bye!"
      
